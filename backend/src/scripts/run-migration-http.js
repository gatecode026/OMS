/**
 * @file src/scripts/run-migration-http.js
 * @description Placeholder – migration script (no-op).
 *              Kept to avoid breaking the import in company.routes.js.
 */

export const runSharedMigration = async () => {
  return { total: 0, seeded: 0, skipped: 0 };
};

export default runSharedMigration;
